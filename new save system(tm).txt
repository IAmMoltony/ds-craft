the current save system sucks
so i will revamp it :)

here is the structure

random-uuid-123-456
  world_meta (meta info like name)
  player_info (player x, y, spawn pos, inventory etc)
  blocks
    chunk_<n>.wld.blk (this will contain info about chunks, currently we only have 1)
  entities
    chunk_<n>.wld.ent (entities for corresponding chunk)
  chests
    chunk_<n>.wld.cst (chest info for corresponding chunk)

format for .wld.blk

<id> <x> <y> [additional info]

format for .wld.ent

<id> <x> <y> <direction> [additional info]

format for .wld.cst

CHEST <id>
<item id> <index> <amount>
...